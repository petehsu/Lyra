# lyra-local-inference-core

Archived experimental FFI adapters for local model inference.

The active runtime still advertises the `local_inference` protocol as
unsupported. Move this crate back into `crates/` only when a real runtime route,
packaging story, and model configuration UI are wired end to end.
