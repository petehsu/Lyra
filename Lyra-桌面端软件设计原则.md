# 专业商用级软件 UI 规则

这是一份可直接放进提示词、设计规范或代码审查标准里的通用规则。它不针对某个具体产品形态，也不规定必须使用某种布局；它描述的是专业商用级桌面/生产力软件应该呈现出的视觉语义、信息密度、交互状态和细节稳定性。

目标不是让界面更“好看”或更“有设计感”，而是让软件显得成熟、可信、克制、精密、长期可用。

## 核心判断

- 专业感来自语义清楚、比例稳定、状态完整、细节一致，而不是装饰。
- 默认界面应该安静，用户操作时才显现更多层级和动作。
- 同一类对象、同一类操作、同一类状态必须长得像同一个系统做出来的。
- 删除不影响任务完成的信息、按钮、说明、徽标、重复标题和重复状态。
- 视觉要服务内容，不要让边框、阴影、颜色、圆角、图标先于内容被看见。
- 任何界面都应先建立“背景 / 内容 / 状态 / 操作”的清晰关系，再考虑局部美化。

## 语义颜色

颜色必须先按语义角色定义，再映射到具体色值。不要在不同页面随手挑近似颜色。

| 角色 | 用途 | 规则 |
| --- | --- | --- |
| 应用背景 | 窗口、主工作区、页面底色 | 稳定、低噪声、低饱和；浅色不灰脏，深色不糊成一片。 |
| 微妙背景 | 弱分组、次级区域、轻量容器 | 只比应用背景略有区分，用来建立层级，不抢内容。 |
| UI 元素背景 | 输入框、按钮、列表行、工具条、可交互区域 | 默认状态要安静，不像一堆卡片。 |
| 悬停元素背景 | hover 状态 | 明确但微妙，只提示“可操作”，不制造强色块。 |
| 活动/选中元素背景 | selected、active、current | 比 hover 更明确，但只使用一种主表达：背景、边框、色调或标记，不要叠满。 |
| 微妙边框和分隔符 | 区域边界、列表分隔、结构线 | 低对比、细、少；能用留白和背景解决时不加线。 |
| UI 元素边框和焦点环 | 输入、选择器、可聚焦控件 | focus 必须可见、可访问、稳定；不要像错误状态一样刺眼。 |
| 悬停元素边框 | hover border | 只轻微增强，不改变元素尺寸，不导致布局跳动。 |
| 纯色背景 | 主按钮、危险操作、强状态、品牌身份 | 少用，只给真正需要强调的对象。 |
| 悬停纯色背景 | solid hover | 只调整明度、透明度或饱和度，不突然换色相。 |
| 低对比文本 | 辅助信息、meta、placeholder、弱说明 | 权重低但仍可读，不能为了“高级灰”牺牲识别。 |
| 高对比文本 | 标题、主要内容、当前值、关键结果 | 少而准；不要让所有文字都同样醒目。 |

## 颜色使用

- 中性颜色是主系统，强调色是例外。
- 彩色只用于真实身份或明确状态，例如品牌、文件类型、网站 favicon、模型/provider、成功、警告、错误、危险。
- 不要用彩色装饰普通结构，不要用渐变、光斑、大片高饱和背景制造“高级感”。
- 状态颜色必须语义稳定：红色表示危险/错误，黄色表示警告，绿色表示成功，蓝色或品牌色表示焦点/主行动/身份。
- 深色模式不是把浅色反相；要重新控制对比、边界和层级，避免脏、糊、灰。

## 层级

- 每个界面只保留必要层级：主内容、次要信息、状态、操作。
- 主内容最清楚，辅助信息降权，装饰性信息删除。
- 同一信息只出现一次；重复标题、重复时间、重复路径、重复状态会让界面显得松散。
- 选中、未读、错误、加载等状态使用最小足够表达：一个点、一个背景、一条提示、一种状态色即可。
- 不要让标题、说明、按钮、图标、边框、徽标同时抢注意力。

## 密度与比例

- 桌面软件应有较高信息密度，但不能拥挤。
- 同类元素必须共享尺寸体系：行高、padding、gap、图标尺寸、圆角、边框、hover/active 状态。
- 不要让每个页面都有自己的宽度、边距、列表高度、工具栏高度和按钮尺寸。
- 固定格式控件必须有稳定尺寸；hover、loading、图标出现、文本变化不应导致布局跳动。
- 紧凑区域使用紧凑字体和行高，不要把局部面板做成 hero 版式。
- 留白用于组织关系，不用于制造空洞。

## 组件

- 同一种控件只允许有一种主实现。按钮、输入框、菜单、列表、标签、面板、弹层不应在各页面重新发明。
- 组件应由语义和状态驱动，而不是靠一次性 CSS 拼出局部效果。
- 普通页面是 surface，重复对象是 row，详情是 panel，菜单/弹层/对话框才是 overlay。
- 避免卡片套卡片、边框套边框、阴影套阴影。
- 可以有局部变化，但变化必须来自明确语义：密度、层级、危险程度、真实身份、当前状态。

## 交互状态

- default、hover、active/selected、focus、disabled、loading、empty、error、success 都必须被设计。
- hover 表示可操作，selected 表示当前对象，focus 表示键盘位置，不要混用。
- 低频操作可以在 hover、右键菜单、更多菜单或详情区出现；高频主操作才常驻。
- 危险操作要可找到，但不要默认抢注意力。
- disabled 不能只靠透明度到看不清；loading 不能造成布局跳动；error 必须说明可行动原因。

## 图标

- 普通 UI 图标使用统一风格的单色线性图标。
- 同一工具区域内图标的尺寸、线宽、视觉重心和对齐方式必须一致。
- 彩色图标只表达真实对象身份或风险状态，不用于装饰普通按钮。
- 彩色图标必须贴近对象自身的真实颜色：品牌接近原厂色，模型/provider 接近官方主色，文件类型使用稳定且可区分的类型色。
- 彩色不是硬编码装饰色；当原色与当前主题背景、文本或选中态对比不足时，必须自动适配、反色或降级为可读的中性色。
- 同一对象在不同位置必须使用同一图标和同一颜色语义，例如同一个文件类型、模型、provider、favicon、终端或应用身份不能换一套图标。
- 普通操作图标保持单色；只有真实对象身份、来源、风险和状态允许彩色。
- 图标帮助识别，不替代结构；不熟悉的图标需要 tooltip 或文本上下文。

## 文案

- 文案像工具，不像广告。
- 能用一个词就不用一句话；能通过结构理解就不写说明。
- 不解释显而易见的控件，不写“这是用来……”式 UI。
- 保留状态、风险、结果、下一步；删除重复说明、弱价值描述和自我介绍式标题。
- 空状态要短，错误要具体，危险操作要明确。

## 精致感检查

- 默认界面是否安静，操作时才出现更多动作？
- 背景、弱背景、元素背景、hover、active、border、focus、文本是否都有语义？
- 同类控件的尺寸、间距、状态是否完全一致？
- 是否有重复标题、重复时间、重复路径、重复状态或重复入口？
- 是否有为了“好看”而存在的颜色、阴影、圆角、边框、卡片？
- hover、selected、focus 是否清楚但克制？
- 删除某个信息或按钮后，任务是否仍能完成？如果能，就删除或降级。
- 浅色模式是否清爽不灰，深色模式是否清楚不脏？
- 界面是否像一个稳定工具，而不是组件 demo、网页 landing page 或玩具应用？

## 默认桌面软件设计提示词

把界面做成专业商用级桌面/生产力软件，而不是网页落地页、组件 demo 或玩具应用。界面应克制、干净、精密、稳定，主内容永远先于装饰被看见。

遵循这些默认规则：

- 用现有设计系统和组件完成界面；不要为单个页面发明新的按钮、输入框、列表、卡片、菜单、弹层、图标风格或颜色体系。
- 颜色必须语义化：背景、弱背景、控件背景、hover、selected、border、focus、文本、危险、警告、成功、品牌/身份各司其职。不要随手写局部颜色。
- 中性颜色是主系统；强调色只给焦点、主行动、真实对象身份和明确状态。普通操作图标用统一单色线性图标；彩色只用于文件类型、favicon、品牌、模型/provider、应用身份、终端身份和风险状态。
- 彩色图标要贴近对象真实颜色，但必须可读；当原色在当前主题下对比不足时，自动反色、提亮、降饱和或降级为中性色。同一对象在输入框、列表、菜单、拖拽、详情区必须使用同一图标和颜色语义。
- 同类区域必须共享同一套密度：侧栏宽度、行高、padding、gap、圆角、图标尺寸、边框、hover、selected、focus 都要一致。不要一个页面一套比例。
- 信息层级只保留主内容、次要信息、状态、操作。同一信息只出现一次；删除重复标题、重复路径、重复时间、重复状态、重复按钮和解释显而易见功能的文案。
- 默认状态要安静；hover 清楚但微妙；selected/active 明确但克制；focus 可访问；disabled、loading、empty、error、success 都必须稳定，不造成布局跳动。
- 页面是完整 surface，重复对象用 row，详情用 panel，菜单/弹层/对话框才用 overlay 和阴影。避免卡片套卡片、边框套边框、阴影套阴影、大面积渐变、装饰光斑、过度圆角和无语义彩色。
- 桌面软件应高密度但不拥挤。固定格式控件必须有稳定尺寸；图标出现、hover、loading、长文本、空状态都不能把布局撑乱。
- 文案像工具，不像广告。能用一个词不用一句话；不写“点击这里”“选择一个文件开始”这类把用户当新手的空话。保留状态、风险、结果和下一步。
- 每个界面都要经得起细看：浅色不灰脏，深色不糊，边界微妙，状态完整，文本不挤，控件不飘，细节像同一个成熟产品做出来的。

最终验收标准：一眼能看出当前对象和主要任务；操作入口自然但不吵；细看时比例、间距、状态、图标、文案和颜色都稳定统一，没有玩具感、拼贴感、临时感。
