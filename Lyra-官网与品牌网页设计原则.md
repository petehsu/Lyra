# Lyra 官网与品牌网页设计原则

本文档来自 Lyra 项目会话 `019f51a0-797d-7282-995e-55eae63f482f` 中对官网、启动页、软件复刻、文案与交互动效的连续设计反馈。

它用于约束后续负责 Lyra 官网、品牌页面、产品介绍页和法律页面的 Agent。目标不是复用某一版页面，而是保留判断标准，避免设计再次退回模板化、AI 味、与真实产品脱节的状态。

本文档应与 `Lyra-桌面端软件设计原则.md` 一起使用：

- 桌面端设计原则负责软件内部的工具界面。
- 本文负责官网、品牌表达、产品演示和网页叙事。
- 官网可以比桌面端更有叙事性，但不能牺牲真实、克制、清晰和性能。

## 1. 产品真相

Lyra 不是一个普通聊天框，也不是又一个只会生成代码的 AI 工具。

Lyra 的核心概念是一个由用户与 Agent 共同使用的工作台：

- 网页、终端、文件、项目和桌面应用处于同一个工作现场。
- 用户可以亲自操作，也可以让 Agent 理解当前工作区并继续完成任务。
- Agent 不只在聊天框里回答，而是在可见、可操作的环境中工作。
- 产品价值来自上下文、工具和工作现场的统一，而不是“有 AI”这件事本身。

官网的每一段文案和每一个视觉演示都必须围绕这个产品事实展开。

如果一段内容换成 Cursor、Zed、Codex、Claude Code 或任意 AI 产品的名字仍然成立，它大概率不是 Lyra 的有效表达。

## 2. 官网不是模板

不要先套 landing page 模板，再把 Lyra 的名字填进去。

禁止默认采用以下结构：

- 大标题、两句空泛说明、两个按钮。
- 三张功能卡片。
- 六个带彩色图标的卖点。
- 一排没有真实来源的数字。
- 一段“重新定义未来”的口号。
- 一个装饰性终端窗口。
- 一组与产品没有直接关系的渐变、光球和玻璃卡片。

页面结构必须从 Lyra 自身的工作方式产生。某个区块如果只是在填满页面，应删除。

## 3. 先读产品，再设计官网

Agent 在设计或修改官网前必须先阅读实际桌面端代码。

至少确认：

- 当前工作台的真实布局。
- AI 面板、网页工作区、终端、文件区和顶部工具栏的关系。
- 当前颜色、材质、边框、圆角、字体和图标体系。
- 产品中已经存在的可交互状态。
- 当前真正可用的功能，而不是计划中的能力。

截图只能辅助理解问题，不能成为官网复刻的主要来源。开发过程中的截图可能尺寸不统一、状态不完整或已经过时。

桌面端代码和实际运行效果才是产品视觉的主要事实来源。

## 4. 真实复刻，而不是截图

官网中的 Lyra 软件演示必须使用 HTML、CSS 和真实组件结构重建，不使用产品截图冒充交互界面。

复刻标准：

- 第一眼应当与真实 Lyra 属于同一个产品。
- 面板比例、工具栏、标签页、输入框、终端和材质关系要接近真实软件。
- 可以为了网页展示降低信息复杂度，但不能改成另一套设计语言。
- 可交互区域应真的可交互，例如切换标签、主题、终端或输入消息。
- 使用真实图标体系和品牌资源，不临时绘制风格不一致的图标。

不要求逐像素复制每一个业务细节，但不允许只做一个“像 IDE 的深色框”就称为 Lyra。

## 5. 首页创意必须来自产品

Lyra 官网首屏的核心叙事是：

> 用户一开始看到的是 Lyra 工作台内部正在浏览的官网。向下滚动时，视角逐渐退出网页工作区，最终看见完整的 Lyra 工作台。

这不是普通的页面缩放特效，而是产品概念的视觉表达：

- 官网本来就在 Lyra 中。
- 网页不是被放进一张产品图里。
- 滚动是在揭示工作环境，而不是切换两个无关区块。
- 页面向下推进，与传统内容向上离场的滚动叙事形成区别。

后续可以调整具体节奏，但不能破坏“从工作区网页退出并看见完整 Lyra”的真实空间关系。

## 6. 单一真实节点原则

同一个网页在同一时刻只能有一份主要 DOM。

禁止：

- 创建一份全屏官网，再创建一份工作台内部官网。
- 用交叉淡入淡出掩盖两份页面的切换。
- 在过渡阶段让两套标题、按钮或导航重叠。
- 用视觉欺骗代替正确的结构关系。

正确实现：

- 官网节点真实位于模拟的 Lyra 网页工作区内。
- 首屏时，完整工作台以更大的真实布局尺寸位于窗口之外，只让网页工作区覆盖视口。
- 滚动时，工作台自身一起缩小并移动到最终居中位置。
- 最终看到的网页仍是首屏中的同一个节点。

遇到复杂动效时，先修正 DOM 所属关系，再写动画。

## 7. 清晰度与缩放

不要对包含文字、边框、半透明材质和 `backdrop-filter` 的完整工作台使用 `transform: scale()`。

复杂 DOM 被整体变换时，Chromium 可能先把子树栅格化，再缩放合成层，导致：

- 文字发糊。
- 细边框失真。
- 半透明材质变脏。
- 不同设备像素比下清晰度不稳定。

首选方案：

- 动画修改真实 `width`、`left` 和 `top`。
- 让浏览器重新布局和绘制文字。
- 使用稳定的 `aspect-ratio`、网格和容器查询维持内部比例。
- 工作台最终尺寸同时受视口宽度和高度约束。
- 最终工作台整体居中，不能只按宽度计算后从底部溢出。

动画过程中必须检查计算样式，确认工作台没有残留 `scale()`。

## 8. 响应式不是缩小桌面截图

每个关键视口都要重新判断构图，而不是把桌面版等比例缩小。

桌面端：

- 首屏网页工作区必须完整覆盖可见视口。
- 滚动后工作台同时适配可用宽度和可用高度。
- 背景、工作台和网页工作区的几何关系必须准确。
- 中间状态不能明显向一侧倾斜或露出无意义空白。

窄屏和移动端：

- 不强行保留不适合触屏的完整桌面动画。
- 可以使用静态产品演示和独立首屏文案。
- 主要内容、下载入口和语言入口始终可见。
- 不允许重要按钮因为窗口高度降低而消失。

至少验证：

- 1920 × 958 附近的宽桌面视口。
- 1280 × 720 的普通桌面视口。
- 约 900px 的断点状态。
- 常见移动端宽度。
- `prefers-reduced-motion`。

## 9. 视觉语言

Lyra 官网应当高级、简洁、专业、大气，但这些词不能靠装饰实现。

正确来源：

- 准确的比例。
- 充足但有目的的留白。
- 稳定的排版层级。
- 克制的中性色。
- 与真实软件一致的材质和细节。
- 有产品含义的动效。
- 少而准确的内容。

避免：

- 紫色或蓝紫渐变成为默认品牌方案。
- 大面积暖米色、咖啡色或单一色相统治页面。
- 渐变文字标题。
- 装饰光球、光斑和无来源的环境光。
- 过度玻璃拟态。
- 巨大阴影和悬浮卡片墙。
- 徽章、药丸标签和圆角方块图标泛滥。
- 卡片套卡片、边框套边框。
- 每个区块都有 eyebrow、kicker 或编号。
- 为了显得“科技”而加入无意义终端。

真实产品中的终端、系统材质和字符 Logo 是产品身份，不属于装饰性 AI 模板元素，但仍要克制使用。

## 10. 品牌元素

### 字符 Logo

- 使用 Lyra 已有的字符风格 Logo。
- 字符 Logo 的扫光应复用产品中已经成立的实现。
- 不添加无关星星、粒子或漂浮装饰。
- 不让 Logo 上下浮动。
- Logo 不需要独立卡片或容器背景。
- 动画要保留字符结构的清晰度。

### LYRA 字标

- `LYRA` 使用既定品牌字体，例如 Zen Dots。
- 品牌字体只用于品牌身份，不扩散到正文和工具界面。
- 字号、字重和 Logo 的比例要在不同视口中稳定。

### 系统材质

- 桌面软件演示中的系统材质应接近真实 Lyra。
- 半透明区域必须仍有清楚的层级，不能所有面板都透成同一层。
- 输入框、菜单、弹窗和终端需要足够实底，保证内容可读。
- 浅色和深色模式都要单独校准，不能只做反色。

## 11. 文案原则

### 不把用户当成需要被讲解的人

不要写：

> 上面的工作台由桌面端真实结构与视觉令牌重新构建。您可以切换标签、主题与终端，也可以在输入框里发送一条消息。

界面能够被操作，就让用户操作。不要在页面上解释设计实现、组件功能或显而易见的交互。

### 不把行业标配当作核心优势

不要把下面这类所有竞品都有的能力单独包装成主卖点：

- 工具调用可见。
- 终端输出可见。
- 文件改动可见。
- 项目和偏好保存在本地。
- 支持多模型。
- AI 可以写代码。

这些内容只有在解释 Lyra 独特工作方式时才有价值，不能脱离上下文成为空泛功能卡片。

### 写具体的产品关系

优先表达：

- 用户与 Agent 共享同一个工作现场。
- 网页、终端、文件和桌面应用不再是割裂的上下文。
- 用户可以随时接手，也可以让 Agent 在眼前继续工作。
- Lyra 是工作台，而不是聊天窗口的另一种皮肤。
- Oma 等能力如何组织 Agent 完成真实任务。

### 语气

- 克制、清楚、有判断。
- 可以有温度，但不做煽情式品牌宣言。
- 不使用“革命性”“重新定义”“无限可能”“下一代”等没有证据的词。
- 不使用“not just X, but Y”式模板句。
- 不使用“AI-powered”作为主要价值说明。
- 不连续使用短促、夸张、口号化的句子。
- 不解释用户已经能看见的内容。

Lyra 是个人长期开发维护的项目。需要表达开发者主体时使用“我”，不要凭空塑造一个不存在的“我们”。

## 12. 多语言

首版官网至少支持中文和英文，并从结构上允许继续扩展。

要求：

- 文案集中管理，不散落硬编码。
- 翻译按语言重新编辑，不逐字直译。
- 中文强调自然、克制和礼貌。
- 英文避免中式语序和营销套话。
- 不同语言可以调整句长和停顿，但保持相同产品事实。
- 导航、按钮、法律页面和元数据都要进入多语言体系。
- 文本长度变化不能破坏布局。

## 13. 动效

GSAP 可以使用，但“极致丝滑”不是堆叠更多动画。

每个动画必须回答：

- 它在解释什么空间关系？
- 它在强调什么产品概念？
- 删除它会不会损失理解？
- 它是否在低性能设备上仍然稳定？

推荐：

- 与滚动进度直接对应的空间揭示。
- 轻微、稳定的进入动效。
- Logo 内部的扫光。
- 主题切换的颜色过渡。
- 不改变布局的文案过渡。

避免：

- 弹簧式 hover。
- 所有元素依次飞入。
- 没有意义的视差层。
- 持续运行的大面积模糊和滤镜。
- 用动画遮掩错误布局。
- 动效导致文本、按钮或页面高度跳动。

必须提供 `prefers-reduced-motion` 的静态替代。

## 14. 性能

视觉效果不能以软件卡顿为代价。

- 避免滚动时对多份复杂 DOM 同时布局和绘制。
- 避免把大型半透明子树强制放进缩放合成层。
- 避免无限运行但用户几乎看不见的动画。
- 避免在每个组件重复监听滚动和窗口变化。
- 使用一个明确的滚动控制器管理首页核心场景。
- 动效中的尺寸和位置变化需要在真实设备上测量，不凭感觉猜。
- 页面内容增加后仍要检查滚动、拖动和交互是否流畅。

性能问题必须找到根因，不能通过删掉产品身份效果临时掩盖。

## 15. 组件与工程

- 组件按职责拆分，不按视觉碎片过度拆分。
- 优先复用项目已有组件和已安装依赖。
- 不为了“现代”引入一整套与 Lyra 不一致的组件库。
- 开源组件只能解决基础行为，最终视觉必须服从 Lyra。
- 不为单次动画建立庞大抽象。
- 复杂场景先保证正确 DOM 结构，再写动画。
- 页面、产品演示、法律内容和多语言数据应保持清楚边界。
- 不修改无关桌面端代码来迁就官网的临时实现。

## 16. 法律页面

用户协议和隐私政策不是占位营销页。

- 内容必须结合 Lyra 的真实登录、Supabase、OAuth、本地数据、远程语言包和 Agent 行为。
- 结构要便于法律审阅和后续更新。
- 支持多语言和独立发布。
- 不使用模板化假公司信息。
- 不承诺当前产品并不具备的安全、合规或服务能力。
- 页面视觉与官网一致，但阅读性高于品牌展示。

正式发布前必须由合适司法辖区的专业人士复核。

## 17. Agent 工作流程

负责 Lyra 官网的 Agent 应按以下顺序工作：

1. 阅读本文档和桌面端设计原则。
2. 阅读官网现有代码和桌面端相关 UI 代码。
3. 运行产品或查看最新实际界面。
4. 明确本次页面要表达的一个产品事实。
5. 删除模板化区块和竞品通用文案。
6. 先建立正确 DOM 与响应式关系，再添加动效。
7. 在中文和英文中分别审校文案。
8. 使用真实浏览器检查首屏、中间滚动状态和最终状态。
9. 检查桌面、窄屏、移动端和 reduced motion。
10. 运行类型检查、生产构建和 AI slop 扫描。
11. 对扫描结果人工判断，不机械删除真实产品元素。
12. 只有实际视觉验证通过后才算完成。

## 18. 验收问题

完成一个页面后逐项回答：

- 这是否一眼就是 Lyra，而不是任意 AI 产品？
- 页面表达了哪个真实且独特的产品事实？
- 是否阅读了真实桌面端代码？
- 产品演示是否为代码实现，而不是截图？
- 是否存在两份相同页面或视觉欺骗？
- 是否对复杂工作台使用了 `transform: scale()`？
- 不同屏幕尺寸下是否仍然清晰、居中和完整？
- 文案是否在解释显而易见的界面？
- 是否把行业标配写成了核心优势？
- 是否出现模板化 AI 文案和默认 AI 配色？
- 动效是否帮助理解产品空间关系？
- 浅色、深色和 reduced motion 是否可用？
- 中文和英文是否都像自然写作？
- 删除某个区块后，页面是否反而更清楚？

任何一个核心问题回答不清楚，都不应直接交付。

## 19. 可直接用于 Agent 的默认提示词

```text
你正在设计或修改 Lyra 的官网。开始前先阅读：

1. Lyra-官网与品牌网页设计原则.md
2. Lyra-桌面端软件设计原则.md
3. web/site 当前实现
4. 与页面有关的真实桌面端 UI 代码

Lyra 是用户与 Agent 共享网页、终端、文件、项目和桌面应用上下文的工作台，不是普通 AI 聊天框。官网必须从这个产品事实出发。

不要套用 AI landing page 模板。不要使用紫蓝渐变、光球、卡片墙、徽章堆叠、虚假统计、装饰终端、渐变标题或“重新定义未来”等空泛表达。不要解释显而易见的界面，不要把所有竞品都有的功能当作 Lyra 的核心优势。

产品演示必须阅读并接近真实 Lyra UI，使用代码重建，不能使用截图代替。需要交互时实现真实交互。首页“从 Lyra 网页工作区退出并看见完整工作台”的场景必须只有一个官网 DOM 和一套工作台结构；工作台自身一起缩小。不要用两份页面交叉淡入，不要对复杂工作台使用 transform: scale()。使用真实 width、left、top、稳定比例和容器查询保持文字清晰。

设计应高级、简洁、专业、克制。高级感来自准确比例、留白、排版、真实材质和有含义的动效，而不是装饰。动效必须解释产品空间关系，并支持 prefers-reduced-motion。

文案要具体、有判断、尊重用户。优先表达 Lyra 独特的工作台关系。需要表达开发者主体时使用“我”，不要虚构“我们”。中文和英文分别自然编辑，不做硬翻译。

实现后必须在真实浏览器验证宽桌面、普通桌面、窄屏、移动端、首屏、滚动中段和最终状态，并运行类型检查、生产构建与 AI slop 扫描。不要只报告代码完成，必须检查实际视觉效果。
```
