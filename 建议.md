这个方向我很认可，而且我觉得你已经抓到核心了：

**先做“可操作骨架”，再做“受控感知”，最后才做少量 DOM 补读。**
这比“整页 DOM 喂给模型”或者“纯视觉识别”都更像一条能长期跑通的自研路线。

我直接说结论：
你的方案是对的，但还需要再补 4 个关键设计，不然会卡在稳定性和泛化上。

---

## 先评价你这套思路

你现在的链路本质上是：

1. **Tab 侧构建操作骨架**
2. **Agent 用选择器在骨架上探索和操作**
3. **信息不足时再局部轻读 DOM**

这套设计的优点非常明显。

### 优点 1：把 token 消耗从“页面理解”挪到“任务决策”

这是最值钱的地方。
如果骨架层能先把页面压缩成：

* 可点击
* 可输入
* 可选择
* 可展开
* 可提交
* 可下载

这些结构化节点，模型就不需要先读一大坨 HTML 才知道能干什么。

也就是说，模型只负责：

* 当前目标是什么
* 该选哪个节点
* 下一步缺什么信息

而不是负责：

* 从杂乱 DOM 里自己做页面解析

这会让成本、时延、稳定性都好很多。

---

### 优点 2：把“感知”变成分层调用

你不是默认读 DOM，而是：

* 先看骨架
* 不够再看选中的元素
* 还不够再轻读局部 DOM

这就是很好的 **progressive disclosure**。

对 Agent 来说，最怕的是：

* 一开始信息过多
* 每一步都重读全页
* 页面一变就要重新理解全量结构

你这个方案天然能避免。

---

### 优点 3：选择器是关键的人机共用抽象

你提到“像开发者面板那个选择器”，这个非常对。
因为真正稳定的不是“模型直接理解页面”，而是你定义一套 **统一可寻址对象模型**。

也就是说，Agent 不应该面对原始网页，而应该面对你定义的这层对象：

* `node_id`
* `role`
* `name`
* `type`
* `state`
* `bounds`
* `children`
* `actionability`

这样 Agent 的世界就从网页变成了“可操作对象图”。

这是能不能平台化的分水岭。

---

## 但你还要补 4 个关键点

---

## 1. 不要只做“骨架”，要做“交互语义骨架”

你现在说的骨架，如果只是：

* button
* input
* div
* a

还不够。

Agent 真正需要的不是标签骨架，而是**交互语义骨架**。

建议每个节点至少包含这几类信息：

### 基础身份

* `node_id`
* `tag`
* `role`
* `name`
* `text`
* `placeholder`
* `label`

### 交互能力

* `clickable`
* `editable`
* `selectable`
* `checkable`
* `expandable`
* `draggable`
* `uploadable`
* `downloadable`

### 当前状态

* `visible`
* `enabled`
* `readonly`
* `checked`
* `selected`
* `expanded`
* `required`
* `invalid`

### 关系信息

* `parent`
* `children`
* `form_owner`
* `label_for`
* `described_by`
* `group`
* `table_row` / `table_col`

### 稳定锚点

* `aria-*`
* `data-testid`
* `data-qa`
* `name`
* `id`
* `href`
* `input type`

重点是：
**Agent 要知道“这个东西能不能操作、现在是什么状态、跟谁有关联”，而不是只知道它长得像什么标签。**

---

## 2. 选择器不要做成“开发者模式”，要做成“Agent 查询语言”

你说的选择器如果只是“框住一个元素看属性”，那还只是人工调试工具。

更好的做法是把它升级成一层 **Agent Selector DSL**。

例如让 Agent 支持这种查询：

```text
find(button, name="登录")
find(input, label="手机号")
find(select, near="国家/地区")
find(node, text~="导出", clickable=true)
find(row, contains=["订单号", "待支付"])
find(dialog, visible=true)
```

再进一步，可以支持组合：

```text
within(form, name="登录表单").find(input, label="密码")
within(table, name="订单列表").find(button, text="导出")
near(text="付款方式").find(select)
```

这样模型不是在“读网页”，而是在“查询一棵交互对象树”。

这会极大提升稳定性，因为：

* 你可以做 selector rewrite
* 你可以做多策略 fallback
* 你可以做 ranking
* 你可以做缓存

而不是每次都让模型从零理解元素。

---

## 3. “轻读 DOM”一定要做成局部、按需、预算受控

你提的第三步非常关键，但一定要控制住，不然最后又滑回“整页 DOM 阅读”。

建议把 DOM 读取分成 4 档：

### 档 1：节点级读取

只读某个选中节点的：

* outerHTML 摘要
* attributes
* computed label
* 事件绑定摘要

### 档 2：邻域读取

只读该节点周围 1~2 层：

* 父容器
* 同组元素
* 相邻 label / hint / error

### 档 3：区域读取

只读某个区域：

* 当前 form
* 当前 dialog
* 当前 table
* 当前 sidebar

### 档 4：页面级读取

只在必要时触发，而且要有 hard budget：

* token 上限
* 节点数上限
* 文本长度上限

建议默认策略：

**能用骨架解决，就不读 DOM；
能用节点邻域解决，就不读区域；
能读区域，就不读全页。**

---

## 4. 要引入“动作验证层”，否则骨架再好也会误操作

很多人做 Agent 浏览器系统时，重心都放在“看懂页面”，但线上最致命的问题其实是：

**点完以后不知道自己是不是成功了。**

所以你这套方案里一定要加一个验证层。

每次动作后都要自动验证，例如：

### click 后验证

* URL 是否变化
* 是否出现 toast
* 是否出现新弹窗
* 按钮状态是否变化
* 是否触发网络请求

### fill 后验证

* 输入值是否真的写入
* 是否触发校验错误
* 是否被格式化改写

### submit 后验证

* 表单错误是否出现
* 请求状态码是否正常
* 页面是否进入下一步
* 下载是否开始

这个层跟骨架层一样重要。
因为 Agent 不只是“选元素”，而是“完成任务”。

---

# 我对你方案的建议升级版

我会把你现在的三段式，升级成五段式：

## 第 1 层：交互骨架生成

Tab 里实时生成一棵轻量交互树，不给模型原始 DOM。

输出不是 HTML，而是类似：

```json
{
  "page": {
    "url": "...",
    "title": "...",
    "main_regions": ["header", "sidebar", "content", "dialog"]
  },
  "nodes": [
    {
      "id": "n42",
      "role": "button",
      "name": "登录",
      "clickable": true,
      "visible": true,
      "enabled": true,
      "parent": "form_1"
    }
  ]
}
```

---

## 第 2 层：Agent Selector

Agent 通过 DSL 查询骨架，不直接碰网页。

比如：

* 找“导出”按钮
* 找登录表单里的密码框
* 找已展开的菜单项
* 找错误提示附近的输入框

这层相当于你系统的“语义路由器”。

---

## 第 3 层：局部深读

当 Selector 返回多个候选、或者语义不足时，再做局部补读：

* 节点属性
* 邻域 DOM
* 容器摘要
* 文本补充
* 可访问性信息

这一步只给模型必要上下文。

---

## 第 4 层：执行与验证

执行动作后立刻做状态确认：

* 页面状态变了吗
* 请求发出去了吗
* 元素状态更新了吗
* 有报错吗

没有验证就不能进入下一步。

---

## 第 5 层：失败恢复

失败后不要直接让模型重新看整页，而是按规则恢复：

* 同义 selector 重试
* 候选元素换一个
* 展开容器再试
* 滚动后再试
* 关闭遮挡层
* 补读邻域
* 最后再重规划

---

# 你最应该提前防的几个坑

## 坑 1：骨架太薄，最后还是不得不频繁读 DOM

如果骨架里只有标签和文本，不包含状态、关系、交互能力，那它就只是“简化版 DOM”，价值有限。

所以骨架一定要尽量语义化。

---

## 坑 2：选择器只支持单点查找，不支持上下文定位

真实网页里最常见的问题不是“找不到按钮”，而是：

* 有 3 个“提交”
* 有 2 个“导出”
* 有多个相同 placeholder

所以选择器必须支持：

* `within`
* `near`
* `after`
* `before`
* `in_dialog`
* `in_table_row`
* `under_menu`

上下文定位能力比单点定位更重要。

---

## 坑 3：SPA 和动态渲染导致骨架过期

React/Vue 页面经常局部刷新。
如果 Agent 拿着旧骨架在操作，就会误点。

所以你要做：

* 增量骨架更新
* 节点版本号
* DOM mutation 监听
* “操作前再确认节点仍然有效”

也就是每个节点最好有：

* `node_id`
* `revision`
* `stable_fingerprint`

---

## 坑 4：隐藏元素、覆盖层、假可点击元素

有些元素在骨架上看似可点，实际上：

* 被遮罩挡住
* 在 viewport 外
* disabled 但样式没体现
* 点击后无事件

所以“可操作”要区分几层：

* `semantically_actionable`
* `actually_visible`
* `hit_test_passed`
* `interactable_now`

最后真正执行前最好做一次 runtime actionability check。

---

## 坑 5：表格、树、级联菜单是难点

普通表单容易做，真正难的是：

* 虚拟列表
* 无限滚动表格
* 树形控件
* 级联下拉
* 富文本编辑器
* iframe
* shadow DOM

建议 MVP 先不要把这些都吃下，先聚焦：

* 登录
* 表单
* 菜单导航
* 普通表格筛选
* 下载导出

---

# 我建议你定义的核心数据结构

## 1. SkeletonNode

```ts
type SkeletonNode = {
  id: string
  role: string
  name?: string
  text?: string
  tag?: string

  visible: boolean
  enabled: boolean

  clickable?: boolean
  editable?: boolean
  selectable?: boolean
  expandable?: boolean

  checked?: boolean
  selected?: boolean
  expanded?: boolean
  invalid?: boolean

  parentId?: string
  childrenIds?: string[]

  labelFor?: string
  groupId?: string
  regionId?: string

  stableAttrs?: Record<string, string>
  rect?: { x: number; y: number; w: number; h: number }
  revision: number
}
```

## 2. SelectorQuery

```ts
type SelectorQuery = {
  role?: string
  name?: string
  text?: string
  state?: Record<string, boolean>
  within?: SelectorQuery
  near?: SelectorQuery
  index?: number
}
```

## 3. PerceptionBudget

```ts
type PerceptionBudget = {
  maxNodes: number
  maxTextChars: number
  maxDomDepth: number
  maxRegionReads: number
}
```

---

# 我对你方案的最终判断

我会给这个方案很高评价，因为它本质上是在做三件特别正确的事：

### 第一，先压缩网页，再给模型

不是让模型替你做浏览器引擎。

### 第二，把网页交互抽象成对象树

不是让模型直接碰原始 DOM。

### 第三，把高成本感知变成按需触发

不是每一步都重读页面。

这三点都很对。

---

# 我会怎么给你定一句产品定义

你这个方案可以定义成：

**一种 DOM-first 的 Browser Agent Runtime：以 Tab 内实时交互骨架为主视图，以语义选择器为操作接口，以局部 DOM 补读为信息增强，以动作验证和恢复机制保证任务闭环。**

这个定义很完整，也适合以后对外讲。

---

# 接下来我建议你先做的事

先别急着做很强的 Agent，先把下面三件做扎实：

1. **骨架 schema 定义**
2. **选择器 DSL 定义**
3. **动作后验证协议**

只要这三件定义得好，后面的模型、策略、恢复都能慢慢叠上去。

你这条路是能成的，而且比“纯视觉 Agent”更像工程上真正可控的路线。

下一条我可以直接帮你继续往下拆成一版更实用的内容：
**“Browser Agent 骨架 schema + Selector DSL + 执行接口设计草案”**。
