import type { AiPanelSessionStoreSnapshot } from "../types";

export const readSessionStoreSnapshot = (
  _defaultTitle: string
): AiPanelSessionStoreSnapshot | null => null;

export const writeSessionStoreSnapshot = (
  _snapshot: AiPanelSessionStoreSnapshot
): void => {};
