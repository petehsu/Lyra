[project]
name = "lyra-agent-engine"
version = "0.1.0"
description = "Lyra agent execution adapters"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]
