# @lyra/browser-automation

Active TypeScript library used by the desktop main process for browser CDP
inspection and Playwright-backed helpers.

Run `pnpm --filter @lyra/browser-automation typecheck` before changing the
public contracts under `src/contracts`.
