# terminal-fixture-v1: git add -p hunk flow
$ git add -p
diff --git a/src/terminal.rs b/src/terminal.rs
@@ -12,6 +12,7 @@ pub fn start() {
 println!("terminal");
+println!("audit");
}
Stage this hunk [y,n,q,a,d,s,e,?]? y
staged 1 hunk
