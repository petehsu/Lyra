[package]
name = "lyra-process-lifecycle-core"
version = "0.1.0"
edition = "2021"
license-file.workspace = true

[dependencies]

[target.'cfg(unix)'.dependencies]
libc = { workspace = true }

[target.'cfg(windows)'.dependencies]
windows-sys = { version = "0.61.2", features = [
  "Win32_Foundation",
  "Win32_System_JobObjects",
  "Win32_System_Threading",
] }
