{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "properties": {
    "restoreFiles": {
      "description": "Restore local file changes to the checkpoint captured before `turn_id`.\n\nWhen true, rollback fails before mutating history if Lyra cannot prove the file state can be restored safely.",
      "type": "boolean"
    },
    "threadId": {
      "type": "string"
    },
    "turnId": {
      "description": "The turn to roll back from. This turn and all newer turns are removed.",
      "type": "string"
    }
  },
  "required": [
    "restoreFiles",
    "threadId",
    "turnId"
  ],
  "title": "ThreadRollbackParams",
  "type": "object"
}