# Prompt Contract Gate audit acknowledgements.
#
# Keep this file small and explicit. A protected prompt/context/memory/Tool-FS
# change may update this file only when the author intentionally reviewed the
# prompt runtime contract and decided a version/snapshot change is not required.

reviewed_prompt_contract = true
reason = "Reviewed the Persona consent gate: when consent is absent or the host callback fails, identity signals and computed Persona are omitted before prompt construction. The existing dynamic Persona section, stable section IDs, template version, provider interfaces, and session schemas are unchanged."
