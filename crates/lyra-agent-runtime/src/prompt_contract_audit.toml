# Prompt Contract Gate audit acknowledgements.
#
# Keep this file small and explicit. A protected prompt/context/memory/Tool-FS
# change may update this file only when the author intentionally reviewed the
# prompt runtime contract and decided a version/snapshot change is not required.

reviewed_prompt_contract = true
reason = "Bumped PROMPT_TEMPLATE_VERSION to 21 — removed all 'Lyra' product name references from prompt templates (replaced w/ neutral terms), removed 'physical self' language (eliminates virtual/physical dualism), rewrote kernel.md.j2 identity line to natural immersive identity (name/age/location/first_used_brief), added location fallback (8 US tech cities hash os_username), added age inference (home_dir_birthtime + git_first_commit_date weak signals, default 21), added first_used_at persistence (NativeStateFile lazy-init), fallback_lyra identity_name now empty instead of 'Lyra'."