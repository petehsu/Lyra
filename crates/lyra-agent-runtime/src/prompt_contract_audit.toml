# Prompt Contract Gate audit acknowledgements.
#
# Keep this file small and explicit. A protected prompt/context/memory/Tool-FS
# change may update this file only when the author intentionally reviewed the
# prompt runtime contract and decided a version/snapshot change is not required.

reviewed_prompt_contract = true
reason = "Bumped PROMPT_TEMPLATE_VERSION to 15 — corrected CodeGraph prompt contract to match the actual Tool-FS exposure: provider-visible access goes through tool_fs_search/inspect/run with /tools/code/* paths, while native codegraph_* handles remain internal runtime targets. projectContext carries resolver/framework/architecture/bridge summary for prompt-time routing, and CodeGraph tool outputs attach staleness metadata without advertising nonexistent provider-visible tools."