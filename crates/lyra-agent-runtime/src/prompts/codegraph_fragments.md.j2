{# P6 dynamic: CodeGraph signal-driven fragments. Only rendered when signals
   resolved by `codegraph_signals_for_prompt` in turns.rs. This is a compressed
   pre-fetch summary — the model should verify with /tools/codegraph/* before
   acting on specifics. Empty neighborhoods produce no output (the section is
   skipped in render_prompt_sections). #}
{% if codegraph_signals.resolvedNeighborhoods is defined and codegraph_signals.resolvedNeighborhoods|length > 0 -%}
<CodeGraph neighborhood summary (pre-fetched this turn; verify with /tools/codegraph/* before acting):
{% for nb in codegraph_signals.resolvedNeighborhoods -%}
- {{ nb.name }} ({{ nb.kind }}{% if nb.file %}, {{ nb.file }}{% if nb.line %}:{{ nb.line }}{% endif %}{% endif %})
{%- if nb.directCallers|length > 0 %}; callers: {% for c in nb.directCallers %}{{ c.name }}{% if not loop.last %}, {% endif %}{% endfor %}{% endif %}
{%- if nb.directCallees|length > 0 %}; callees: {% for c in nb.directCallees %}{{ c.name }}{% if not loop.last %}, {% endif %}{% endfor %}{% endif %}
{%- if nb.impactDepth2Count is defined and nb.impactDepth2Count > 0 %}; impact@depth2: {{ nb.impactDepth2Count }} symbol(s){% endif %}
{% endfor -%}
{% endif -%}
{% if codegraph_signals.staleFilesRelevant is defined and codegraph_signals.staleFilesRelevant|length > 0 -%}
CodeGraph warning: these files you mentioned have un-indexed changes; graph data may be stale: {{ codegraph_signals.staleFilesRelevant|join(", ") }}
{% endif -%}
>