# Web Shared Assets

该目录用于 `web/docs` 和后续网页入口的复用资源。

## 子目录
1. `tokens/`：品牌设计 token（颜色、间距、圆角、排版等）。
2. `icons/`：品牌图标与徽标资源。
3. `mdx-components/`：文档内容组件（提示块、API 表、步骤卡片等）。

## 约束
1. 只放“可复用、框架无关或低耦合”的资产。
2. 业务页面实现不要放在这里。
