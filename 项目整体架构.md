# Lyra Workbench 项目整体架构（v1 决策稿）

版本：v1.0  
状态：已拍板（用于启动开发）  
日期：2026-03-26

## 0. 一页结论（先看这个）

1. 产品定位：`AI 原生工作台`，不是“AI IDE + AI 浏览器”的简单拼接。
2. 技术基座：`Electron + Code-OSS`（工作台壳层）+ `Rust Daemon`（高权限与系统能力层）。
3. AI 执行引擎：`OpenHands` 作为主执行引擎，`smolagents` 作为轻量可替换执行器。
4. 参考但不强集成：`Codex` 仅作为设计参考，不作为 v1 核心依赖。
5. 浏览器能力：内置 Chromium 面板（WebContentsView），自动化走 `Playwright + CDP`。
6. 插件协议：对外 `MCP-compatible`，并扩展本地权限与审计能力。
7. 商业策略：产品闭源；协议、SDK、基础桥接模板开放（Open Core）。
8. v1 目标：先做“单机可用 + 安全可控 + 可扩展”的工作台内核，再做生态放大。

---

## 1. 产品定义

## 1.1 我们在做什么

Lyra Workbench 是一个统一工作台，AI 可以在同一上下文中使用这些能力：

- 代码编辑
- 终端执行
- 浏览器操作与观测
- 文件管理和多格式查看
- 通过插件操作外部软件

## 1.2 不做什么（v1）

- 不 fork Chromium/Gecko 内核
- 不做云端协作优先
- 不做“全自动无人监管”模式
- 不追求一次性兼容所有第三方 IDE 扩展

---

## 2. 架构原则

1. 单一事实源：所有可执行能力统一走能力总线（Capability Bus）。
2. 权限前置：高风险动作必须经过权限系统与审计系统。
3. 可替换 AI：模型与执行器可插拔，不锁死单一供应商或框架。
4. 本地优先：核心能力在本机可运行，云只做增强。
5. 分层隔离：UI 层不直接触达高权限系统调用。

---

## 3. 技术选型（已定）

## 3.1 主栈

- 桌面壳层：Electron
- 工作台前端：TypeScript（React + Code-OSS 工作台能力）
- 系统服务层：Rust（`lyrad`）
- Agent 执行层：Python（OpenHands / smolagents）
- 浏览器自动化：Playwright + CDP
- 插件协议：MCP-compatible（JSON-RPC）

## 3.2 集成项目取舍（你已克隆的仓库）

1. `OpenHands`：纳入主路线（主执行引擎）
2. `smolagents`：纳入备选路线（轻量执行引擎）
3. `continue`：纳入参考路线（Provider 抽象、IDE 集成组织方式）
4. `cline`：纳入参考路线（审批流、工具调用 UX、MCP 工作流）
5. `codex`：降级为参考，不作为 v1 核心依赖

Codex 降级原因（工程视角）：

- 当前主定位仍偏 CLI 产品
- App Server SDK 文档明确为实验态（API/打包策略仍可能变动）
- 与你“独立闭源产品 + 强可控集成”的目标存在额外耦合成本

---

## 4. 总体架构（分层）

```text
+-----------------------------------------------------------+
|                  Lyra Workbench (Electron)               |
|  +-------------------+  +-------------------------------+ |
|  | Workbench UI      |  | Agent Control Plane (TS)      | |
|  | - panes/layout    |  | - planning/orchestration      | |
|  | - editor/browser  |  | - approval workflow           | |
|  | - terminal/viewer |  | - context assembly            | |
|  +-------------------+  +-------------------------------+ |
|              | IPC / Capability Bus (JSON-RPC)           |
+--------------|--------------------------------------------+
               |
+--------------v--------------------------------------------+
|                    lyrad (Rust Daemon)                   |
| - file/index/search   - process/pty   - secret vault     |
| - permissions         - sandbox        - audit/replay     |
+--------------|-------------------------|------------------+
               |                         |
   +-----------v-----------+   +---------v------------------+
   | Agent Engine Service  |   | Browser Automation Service |
   | (OpenHands/smolagents)|   | (Playwright + CDP)         |
   +-----------------------+   +----------------------------+
               |
   +-----------v--------------------------------------------+
   | External Plugins / MCP Servers / Third-party Bridges   |
   +--------------------------------------------------------+
```

---

## 5. 模块边界（必须遵守）

## 5.1 Workbench UI（TS）

职责：

- 窗口布局、面板系统、任务可视化、审批 UI
- 不直接执行高权限命令
- 所有动作通过 Capability Bus 发起

禁止：

- 直接访问系统敏感路径
- 直接管理长期密钥

## 5.2 Agent Control Plane（TS）

职责：

- 任务拆分、上下文装配、工具路由
- 管理“计划 -> 执行 -> 回滚 -> 人工确认”流程
- 统一调度 OpenHands / smolagents

## 5.3 lyrad（Rust）

职责：

- 文件、进程、PTY、索引、审计、权限判定
- 插件隔离执行与资源限额
- 密钥托管（系统 keychain + 本地加密）

## 5.4 Agent Engine Service（Python）

职责：

- 运行 AI 执行器（默认 OpenHands）
- 暴露统一执行接口给 Control Plane
- 产出结构化事件流（step、tool_call、approval_needed、result）

## 5.5 Browser Automation Service

职责：

- 操作浏览器、采集 DOM/console/network/screenshot
- 给 Agent 提供“可解释的浏览器状态”，而非纯截图盲操

---

## 6. 协议边界（Capability Bus）

统一三类对象：

1. `resources`：可读上下文（文件、页面状态、日志、会话）
2. `tools`：可执行动作（读写、命令、导航、插件动作）
3. `events`：过程事件（审批请求、状态变化、告警）

最小方法集：

- `resources/list`, `resources/read`
- `tools/list`, `tools/call`
- `events/subscribe`, `events/unsubscribe`

说明：

- 对外协议兼容 MCP
- 对内增加权限元数据：`risk_level`, `scope`, `requires_approval`, `audit_tag`

---

## 7. 插件架构（v1）

## 7.1 插件类型

1. UI 插件：新增面板、视图、命令（低权限）
2. 能力插件：新增工具能力（中高权限）
3. 外部桥接插件：连接第三方软件/API（高权限）

## 7.2 插件清单（manifest）最小字段

- `id`, `version`, `entry`
- `permissions`（fs/net/proc/browser/secrets）
- `risk_level`
- `signing`
- `mcp_capabilities`

## 7.3 安全规则

- 默认最小权限
- 高风险工具必须审批
- 所有工具调用写入审计日志（可回放）

---

## 8. 安全与合规架构（v1 硬要求）

1. 权限分级：`read` / `write` / `execute` / `external`
2. 审批策略：`never` / `on-risk` / `always`
3. 秘钥策略：UI 不见明文，全部经 `lyrad` 代理注入
4. 审计策略：每次任务保留完整事件链与 diff 快照
5. 回滚策略：文件变更和命令执行支持任务级回滚

---

## 9. 商业化与许可证策略（已定方向）

1. 产品闭源：Workbench、控制面、商业功能闭源
2. 生态开放：插件 SDK、协议文档、基础模板开放
3. 许可证红线：v1 不引入 GPL/AGPL 到核心闭源分发链路
4. Source-available 组件（如带 enterprise 目录）仅用其允许部分

---

## 10. 代码仓库目标结构（建议）

```text
Lyra/
  apps/
    desktop/                 # Electron + Workbench UI
  services/
    control-plane/           # TS orchestration
    agent-engine/            # Python OpenHands adapter + smolagents adapter
    browser-automation/      # Playwright/CDP service
  crates/
    lyrad/                   # Rust daemon
    lyra-sandbox/            # sandbox runtime helpers
  packages/
    capability-protocol/     # shared schema/types
    plugin-sdk/              # plugin sdk (ts)
  docs/
    architecture/
    adr/
```

---

## 11. v1 里程碑（12 周）

## Phase 1（W1-W2）：内核起步

- 建立 monorepo 与多进程启动链路
- 打通 UI -> Capability Bus -> Rust Daemon

## Phase 2（W3-W4）：基础工作台

- 文件管理器、编辑器、终端、基础 viewer
- 统一任务事件总线

## Phase 3（W5-W6）：浏览器能力

- 内置浏览器 pane
- Playwright/CDP 自动化与观测通道

## Phase 4（W7-W8）：AI 执行接入

- 接入 OpenHands（主）
- 接入 smolagents（备）
- 人工审批闭环跑通

## Phase 5（W9-W10）：插件化

- 插件 manifest、权限模型、签名校验
- MCP-compatible 工具接入

## Phase 6（W11-W12）：稳定化与内测

- 审计回放、回滚能力
- 性能和崩溃恢复
- 内测文档与安装包

---

## 12. 验收标准（v1）

1. AI 可在一次任务中跨文件、终端、浏览器完成闭环执行
2. 所有高风险动作都有审批与审计记录
3. 插件可在不改主程序的前提下新增能力
4. 关键模块可替换（至少支持两种 AI 执行器）
5. 单机模式可独立运行

---

## 13. 当前冻结决策（本阶段不再反复讨论）

1. 不 fork Chromium/Gecko
2. 不以 Codex 作为核心运行时依赖
3. 主执行引擎先用 OpenHands
4. 工作台基座走 Electron + Code-OSS 路线
5. v1 先做本地单机能力，云端增强后置

---

## 14. 性能架构（平台级硬要求）

## 14.1 先定核心思路：能力优先，不是 UI 优先

对外部软件（例如 v2Ray、文件管理器、解压工具），优先接入“能力插件”而不是完整 GUI 镜像嵌入。  
也就是 AI 主要通过结构化工具调用操作软件，UI 仅做结果展示与人工交互。

收益：

- 大幅降低多标签并发时的渲染开销
- 避免“每个软件都变成一个重 WebView 进程”
- 更容易审计、回放和权限控制

## 14.2 插件运行分级（必须分层）

1. `Light`：纯前端 UI 插件（低 CPU/内存，默认可挂起）
2. `Service`：本地服务插件（独立进程，支持长期运行）
3. `Bridge`：外部软件桥接插件（通过 CLI/API/RPC 操作目标软件）

规则：

- 默认插件都必须可“无损暂停/恢复”
- 长驻插件必须显式声明 `always_on`
- 高风险或高资源插件必须进入独立沙箱进程

## 14.3 标签页生命周期状态机（借鉴浏览器但更激进）

每个标签页（插件页）都处于以下状态之一：

1. `Hot`：当前激活页，满性能运行
2. `Warm`：最近活跃页，低频刷新
3. `Cold`：冻结 JS/定时器/渲染，保留快照
4. `Discarded`：释放 renderer，仅保留可恢复状态

调度策略（默认）：

- 同时 `Hot` 只允许 1 个
- `Warm` 上限 2-3 个（LRU）
- 其余全部 `Cold` 或 `Discarded`
- 内存压力超阈值时，优先从最老 `Cold` 开始 `Discard`

## 14.4 资源治理（Resource Governance）

由 `lyrad` 统一做资源预算与熔断：

- CPU 配额：按插件类型和前后台状态分配
- 内存上限：按插件声明 + 全局压力动态收缩
- IO/网络限速：后台插件默认降速
- 看门狗：超限插件自动降级、暂停或重启

每个插件 manifest 增加：

- `resource_class`: `light | normal | heavy`
- `memory_limit_mb`
- `cpu_quota_percent`
- `background_policy`: `throttle | freeze | keepalive`

## 14.5 AI 回路性能优化（避免 AI 把机器拖死）

1. Context 预算：每轮只注入“必要增量上下文”
2. 工具优先级：先结构化工具结果，再截图兜底
3. 浏览器观测优先 DOM/console/network，避免连续高频截图
4. 长任务分片：任务切片执行，阶段性提交中间结果
5. 并发上限：同一工作区默认限制 Agent 并发数

## 14.6 v1 性能基线（可验收）

1. 冷启动（到可交互）：<= 3 秒（标准开发机）
2. 标签切换：P95 <= 150ms
3. 空闲后台 CPU：<= 8%
4. 常规场景内存（10 个标签页，含 3 个服务插件）：<= 2.5GB
5. 任一插件崩溃不拖垮主进程，5 秒内可恢复

## 14.7 可观测性（必须先做，不然后期一定失控）

全链路指标：

- `tab_state_transitions_total`
- `plugin_cpu_seconds_total`
- `plugin_memory_rss_mb`
- `agent_turn_latency_ms`
- `tool_call_success_rate`
- `discard_recover_latency_ms`

工程要求：

- 默认打开性能采样（可降级）
- 每次版本发布附带性能回归报告
- 发现回归超过阈值时阻断发布

## 14.8 生态扩张期策略（v2+）

1. 插件分仓与签名发布
2. 热门插件白名单预编译/预加载
3. 重型插件迁移到独立 Worker 池或容器池
4. 引入“插件性能评分”，低分插件限制上架和默认权限

---

## 15. 关联文档

- UI 产品蓝图：`/home/lyra/Documents/Lyra/UI产品蓝图.md`
