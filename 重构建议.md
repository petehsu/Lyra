现在我对 Lyra 当前的协议状态有了精确认识。以下是基于实际代码和 7 个项目对比的判断。

---

## Lyra 当前协议的精确状态

| 层 | 现状 | 证据 |
|---|------|------|
| **IPC 方法** | 67 个 `lyra:agent/*` channel，扁平字符串分发，无分组无版本 | `desktop-bridge.ts` L611-675 |
| **事件总线** | `mpsc::channel<Value>`，payload 是 `serde_json::Value`，无 schema | `event_bus.rs` — `publish(Value)` / `subscribe() -> Receiver<Value>` |
| **事件映射** | `KernelTurnEvent` (5 变体) → JSON with `kind` 字符串，手工 `serde_json::json!()` | `event_mapper.rs` — 5 个 `json!()` 宏 |
| **权限/澄清** | IPC 方法 (`agentPermissionRespond` / `agentClarificationRespond`)，不是事件 | `desktop-bridge.ts` L649-650 |
| **协议版本** | 只有 `PROMPT_POLICY_VERSION = 3` / `PROMPT_TEMPLATE_VERSION = 7`，通信协议无版本 | `prompt_contract.rs` L4-5 |
| **TS↔Rust 类型同步** | 手工维护，TS interface 在 `agent.ts` (1456 行)，Rust 端是 `serde_json::Value` | 无代码生成，无 schema 校验 |

---

## 要做什么：按商业优先级排序

### 1. 事件类型 schema + 代码生成（最高优先级）

**问题**：`event_bus.rs` 的 payload 是 `serde_json::Value`，Rust 端发出的事件和 TS 端的消费完全靠手工约定。`event_mapper.rs` 里 5 个 `json!()` 宏手写字段名，TS 端靠 `agent.ts` 里的 interface 匹配。任何一端改了字段名，另一端在运行时才发现——对商业产品来说是不可接受的。

**做法**：在 Rust 端定义强类型 event enum，用 serde derive 序列化，TS 端从序列化产物生成类型。

具体路径：

- Rust 端：把 `event_mapper.rs` 的 5 个手工 `json!()` 替换为一个 `AgentEvent` enum，每个变体 derive `Serialize`。`event_bus.rs` 的 `publish()` 接收 `AgentEvent` 而不是 `Value`。
- TS 硾端：用 `ts-rs` 或 `typeshare` 从 Rust enum 生成 TypeScript discriminated union。或者更简单——定义一个 JSON Schema，两端都从 schema 生成。
- 版本化：每个 event 携带 `schemaVersion: u32`，消费者遇到不认识的版本可以降级处理而不是崩溃。

**参考**：Cline 的 `AgentRuntimeEvent` 14 变体联合类型就是这种模式——每个变体都带 `snapshot: AgentRuntimeStateSnapshot`，消费者拿到事件就有完整上下文。Codex 的 `EventMsg` enum 也是 Rust 强类型 + serde 序列化。

**不做的事**：不引入 Protobuf。Lyra 是同进程 Electron + N-API，不是跨进程微服务。Protobuf 的价值在跨语言跨进程，对 Lyra 是过度工程。serde JSON + TS 代码生成就够了。

### 2. 协议版本号（最高优先级，最小改动）

**问题**：67 个 IPC channel 没有任何版本标识。当你修改某个 channel 的参数结构时，旧的前端代码（比如已发布的桌面版本）会以旧格式调用新后端，`serde_json::Value` 反序列化不报错但字段缺失，导致 silent failure。

**做法**：

- 在 `prompt_contract.rs` 旁边加一个 `protocol_contract.rs`，定义 `PROTOCOL_VERSION: u32`。
- 每个 IPC invoke 的响应体携带 `protocolVersion` 字段。
- 前端启动时检查 `protocolVersion`，不匹配时提示升级而不是继续运行。
- 事件流第一个事件发 `protocol_hello`，携带版本号和能力声明。

这个改动极小——一个常量 + 一个字段——但它是所有后续协议演进的基石。没有它，任何 breaking change 都是 blind migration。

### 3. 权限/澄清从 IPC 方法升级为协议事件（中高优先级）

**问题**：当前 `agentPermissionRespond` 和 `agentClarificationRespond` 是 IPC 方法调用。这意味着权限决策只能从 Electron 渲染进程发起。如果未来要支持：
- 手机 app 远程控制 agent
- Web 界面 attach 到 agent session
- 多个 UI 客户端同时连接

权限决策就没有路径到达 agent runtime。

**做法**：

- `event_mapper.rs` 已经有 `permissionRequired` → `permissionRequested` 的映射，说明 Rust 端已经在发事件了。但响应路径是 IPC method 而不是事件。
- 加一个 `AgentEvent::PermissionRequest { request_id, tool_name, input, options }` 事件，和一个 `PermissionResponse { request_id, decision }` 的客户端→服务端消息。
- IPC method `agentPermissionRespond` 保留作为 Electron 渲染进程的便捷路径，但底层转换为 `PermissionResponse` 消息。

**参考**：Claude Code 的 `can_use_tool` control_request 就是这个模式——server 发请求，client 回响应，用 `request_id` 关联。Zed 的 ACP `AuthorizationKind` + `PermissionOptions` 更进一步，支持多选项下拉和通配符模式。

### 4. 会话持久化 + 崩溃恢复（中高优先级）

**问题**：Lyra 的 agent session 状态在 `state.rs` 的 `OnceLock<Mutex<NativeRuntimeState>>` 里——进程内内存状态。Electron 主进程崩溃或用户重启后，所有 agent session 丢失。

**做法**：

- Session metadata（id、workingDir、createdAt、model、provider）持久化到 `~/.lyra/sessions/` 下的 JSON 或 SQLite。
- 对话消息流持久化为 JSONL（像 Claude Code 的 transcript 文件）——每条消息 append 一行，崩溃时最多丢最后一条。
- 启动时扫描持久化目录，恢复 session 列表（不一定恢复运行中状态，但至少恢复对话历史）。
- `agentSessionList` IPC 方法从持久化存储读取，不从内存状态读取。

**参考**：Claude Code 的 JSONL transcript + UUID/parentUuid 链；Cline 的 SQLite `SessionRow` + 谱系字段。Lyra 不需要 Cline 那么复杂的谱系（parentSessionId / agentId / isSubagent），但基础的 session 持久化和对话历史恢复是商业产品的底线。

### 5. 传输层抽象（中优先级，取决于产品路线图）

**问题**：当前 agent 协议硬绑定在 Electron IPC 上。`ipcRenderer.invoke("lyra:agent/turn/start", ...)` 是唯一的前端→后端路径。如果要做远程访问（手机控制桌面 agent、Web 端 attach session），IPC 无法复用。

**做法**：

- 定义一个 `AgentTransport` 接口（类似 Claude Code 的 `Transport`），有 `send()` / `onEvent()` / `close()`。
- Electron IPC 是一个实现，WebSocket 是另一个实现。
- 前端代码只依赖 `AgentTransport` 接口，不直接调 `ipcRenderer.invoke`。
- 后端 `event_bus.rs` 的 `subscribe()` 已经是 `Receiver<Value>` —— 改成 `Receiver<AgentEvent>` 后，WebSocket 实现只需要把 event 序列化为 JSON 发出去。

**判断**：这一步只有在远程访问是近期产品目标时才做。如果 Lyra 的定位是纯桌面应用，这一步可以推迟。但传输层抽象的成本不高——一个 interface + 一个现有实现的包装——而且它让前 4 项工作的价值成倍放大（因为前 4 项定义的 event schema 和协议版本可以无缝用于任何传输层）。

### 6. 不做的事

| 不做 | 原因 |
|------|------|
| **Protobuf / gRPC** | 同进程 N-API，不需要跨进程 RPC。Cline 用 gRPC 是因为 VSCode 扩展进程和 Core 进程分离。 |
| **ACP 标准合规** | ACP 是 agent 互操作标准，价值在外部 agent 接入。Lyra 的卖点是内置 agent 控制整台电脑，不是让外部 agent 接入。除非要接入 Zed 或其他 ACP 客户端。 |
| **多 agent 编排协议** | Cline 的 team/session 谱系、Claude Code 的 subagent fork——这些都是多 agent 场景。Lyra 当前是单 agent per session，不需要。 |
| **完整的事件快照随传** | Cline 每个 event 都带 `AgentRuntimeStateSnapshot`。Lyra 的事件已经带 `sessionId` + `turnId`，前端可以按需查询完整状态。全量快照会增加每条事件的 payload 大小。 |

---

## 优先级总结

```
P0 (现在就该做):
  1. AgentEvent enum + TS 代码生成     — 消除 TS↔Rust 类型漂移
  2. PROTOCOL_VERSION                  — 一个常量，为所有后续演进铺路

P1 (商业产品底线):
  3. 权限/澄清升级为协议事件            — 为非桌面客户端打开通路
  4. 会话持久化 + 崩溃恢复             — 用户不能因为崩溃丢失对话

P2 (取决于产品路线图):
  5. AgentTransport 抽象               — 远程访问的前置条件
```