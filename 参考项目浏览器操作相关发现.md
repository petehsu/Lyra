# 参考项目浏览器操作相关发现

更新时间：2026-04-12

## 结论摘要

在 `/Users/petehsu/Documents/Lyra/参考` 下，和“浏览器操作 / 网页自动化 / 浏览器 Agent”最相关的项目可分为三档：

- 强相关：项目核心能力就是浏览器操作或明确集成浏览器自动化工具。
- 中相关：有浏览器相关依赖或示例，但不是主线能力。
- 弱相关：主要是 E2E 测试依赖（Playwright 等），不适合作为浏览器操作引擎主参考。

同时，Lyra 自身当前状态已经不是“只在研究这些项目”，而是已经完成了 `browser-use` 的正式并行集成。

当前可以准确描述为：

- `browser-use` 已经正式介入 Lyra 浏览器操作体系。
- 它不是替换现有 `workbench.web_*` 的方案，而是新增的一套并行方案。
- Lyra 当前浏览器操作架构是：`原生 workbench.web_* 引擎 + browser_use.* bridge` 双路径并存。

---

## Lyra 当前落地状态

### 已完成

1. `browser_use.*` 已作为独立工具族接入 Lyra。
2. `current_tab` 已升级为真实 CDP bridge，不再只是本地 delegate 伪装。
3. `managed` 模式已支持 `prompt_real_profile` 的任务内授权流，不再只报 `profile_required`。
4. Rust 侧已有 browser strategy / prefilter / ranking，用于和原生 `workbench.web_*` 并行仲裁。

### 尚未变成的东西

1. `browser-use` 没有替换掉 Lyra 原生网页操作引擎。
2. 当前不是“所有网页操作默认都走 browser-use”。
3. 当前也不是“只剩 browser-use 一套方案”。

### 准确定位

截至这次整理，Lyra 的浏览器操作架构应描述为：

- `Lyra 原生引擎`：继续承担低延迟、当前可见页、selector-first 的快路径。
- `browser-use bridge`：作为并行增强方案，承担更完整的浏览器会话与多步能力。
- `策略层`：由代码仲裁 + 模型选择共同决定优先走哪一套。

---

## 强相关（优先参考）

### 1) browser-use

结论：核心就是浏览器自动化（CDP、元素交互、Agent 浏览器操作）。

对 Lyra 的状态标记：

- `已正式并行集成`
- `已接入独立 browser_use.* tool surface`
- `current_tab 已接真实 CDP bridge`
- `managed 模式已接任务内 real profile 选择`
- `当前定位：并行增强方案，不替换 Lyra 原生 workbench.web_*`

证据文件：

- `/Users/petehsu/Documents/Lyra/参考/browser-use/README.md`
- `/Users/petehsu/Documents/Lyra/参考/browser-use/pyproject.toml`
- `/Users/petehsu/Documents/Lyra/参考/browser-use/browser_use/actor/README.md`

### 2) OpenManus

结论：明确集成 `browser-use` 与 `playwright`，有 Browser Automation Tool。

证据文件：

- `/Users/petehsu/Documents/Lyra/参考/OpenManus/README.md`
- `/Users/petehsu/Documents/Lyra/参考/OpenManus/requirements.txt`

### 3) Rust核心Agent（adk-browser）

结论：Rust 侧浏览器自动化工具集，基于 WebDriver / Selenium，和 Lyra 当前技术方向高度贴近。

证据文件：

- `/Users/petehsu/Documents/Lyra/参考/Rust核心Agent/adk-rust/adk-browser/README.md`
- `/Users/petehsu/Documents/Lyra/参考/Rust核心Agent/adk-rust/adk-browser/Cargo.toml`
- `/Users/petehsu/Documents/Lyra/参考/Rust核心Agent/adk-rust/README.md`

### 4) cline

结论：文档明确包含 Computer Use 浏览器交互能力（点击、输入、滚动、截图等）。

证据文件：

- `/Users/petehsu/Documents/Lyra/参考/cline/README.md`

### 5) agenticSeek

结论：使用 Selenium 与 stealth 方案，明显是浏览器自动化路线。

证据文件：

- `/Users/petehsu/Documents/Lyra/参考/agenticSeek/README.md`
- `/Users/petehsu/Documents/Lyra/参考/agenticSeek/pyproject.toml`

### 6) crewAI（tools 层）

结论：主项目不是浏览器产品，但工具生态里有 Stagehand、SeleniumScrapingTool、Browserbase 相关能力。

证据文件：

- `/Users/petehsu/Documents/Lyra/参考/crewAI/lib/crewai-tools/README.md`
- `/Users/petehsu/Documents/Lyra/参考/crewAI/lib/crewai-tools/src/crewai_tools/tools/stagehand_tool/README.md`
- `/Users/petehsu/Documents/Lyra/参考/crewAI/lib/crewai-tools/pyproject.toml`

### 7) agent-framework

结论：有明确的 Computer Use 示例，适合参考通用 Agent 框架接入方式。

证据文件：

- `/Users/petehsu/Documents/Lyra/参考/agent-framework/dotnet/samples/02-agents/AgentsWithFoundry/Agent_Step15_ComputerUse/README.md`

---

## 中相关（可按需参考）

### 1) MetaGPT

结论：有 Playwright/Selenium 相关依赖和网页示例，但不是项目主线能力。

证据文件：

- `/Users/petehsu/Documents/Lyra/参考/MetaGPT/requirements.txt`

### 2) aider

结论：存在 Playwright 依赖，需二次确认其在项目里的实际角色。

证据文件：

- `/Users/petehsu/Documents/Lyra/参考/aider/pyproject.toml`

### 3) smolagents

结论：存在 Selenium 依赖，但仓库主线不是浏览器操作引擎。

证据文件：

- `/Users/petehsu/Documents/Lyra/参考/smolagents/pyproject.toml`

---

## 弱相关（主要是测试依赖）

以下项目里出现的 Playwright/Puppeteer 以测试或附属用途为主，不建议当作浏览器操作引擎主参考：

- OpenHands
- gemini-cli
- continue
- goose
- open-agent
- opencode
- pearai-app
- void

对应证据主要在 `package.json` 与 E2E 测试说明中。

---

## 易误判项

### ChatDev

`README` 中的 “puppeteer-style” 语义是多 Agent 协作范式，不等同于 Puppeteer 浏览器自动化实现本身，不能直接当作网页操作引擎参考。

证据文件：

- `/Users/petehsu/Documents/Lyra/参考/ChatDev/README.md`

---

## 对 Lyra 的建议参考顺序

建议优先深挖以下四个：

1. `browser-use`
2. `Rust核心Agent/adk-browser`
3. `OpenManus`
4. `cline`

理由：这四个最贴近 Lyra 当前“可见化元素选择 + 动作执行 + 结构化诊断 + Agent 编排”的目标。
